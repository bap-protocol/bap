{
  "name": "@bap-protocol/cli",
  "version": "0.1.0-draft.0",
  "description": "Browser Agent Protocol — command-line tool for inspection and demos.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "bap": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@bap-protocol/core": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
